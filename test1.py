import cv2

#Import only if not previously imported
import cv2
img = cv2.imread("./test1.png",0)     #(flag = 0 or 1 or -1)

#Import only if not previously imported
import cv2
cv2.imshow("Wi121212112me", img)
cv2.waitKey(0)
cv2.destroyAllWindows()